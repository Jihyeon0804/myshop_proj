package com.myshop.option.bo;

import org.springframework.stereotype.Service;

@Service
public class OptionBO {

	/*
	 * @Autowired private OptionRepository optionRepository;
	 * 
	 * public List<OptionEntity> getOptionList(int optionId) { return
	 * optionRepository.findByOptionId(optionId); }
	 * 
	 * public OptionEntity getOption(int id) { return
	 * optionRepository.findById(id).orElse(null); }
	 */
}
